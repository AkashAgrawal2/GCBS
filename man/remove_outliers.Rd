% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_outliers.R
\name{remove_outliers}
\alias{remove_outliers}
\title{Remove Outliers from a Data Frame}
\usage{
remove_outliers(
  data,
  outlier_cols = NULL,
  how = c("any", "all"),
  quiet = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble with one or more \code{*_outlier} logical columns.}

\item{outlier_cols}{Character vector of columns to use for outlier removal. Default: all columns ending in \code{"_outlier"}.}

\item{how}{Should rows be removed if they are outliers in \code{"any"} of the specified columns (default), or \code{"all"} of them?}

\item{quiet}{Logical. If TRUE, suppresses informational messages.}
}
\value{
Tibble without the rows considered outliers, with a summary attached as the "outlier_removal_summary" attribute.
}
\description{
Removes rows flagged as outliers by the logical columns produced by \code{detect_outliers()}.
}
\examples{
df <- tibble::tibble(
  height = c(160, 165, 170, 171, 168, 250),
  weight = c(60, 65, 62, 1000, 64, 61)
)
flagged <- detect_outliers(df)
cleaned <- remove_outliers(flagged)

}
