% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_stepwise_regression_plot.R
\name{auto_stepwise_regression_plot}
\alias{auto_stepwise_regression_plot}
\title{Automatic Stepwise Multiple Linear Regression with Diagnostics Plots}
\usage{
auto_stepwise_regression_plot(
  data,
  outcome,
  predictors,
  stepwise = TRUE,
  direction = c("both", "backward", "forward"),
  criterion = c("AIC", "BIC"),
  add_diagnostics = TRUE
)
}
\arguments{
\item{data}{Data frame or tibble.}

\item{outcome}{Name of numeric outcome variable (character).}

\item{predictors}{Character vector of predictors (can mix numeric/factor).}

\item{stepwise}{Logical; if TRUE, performs backward/forward stepwise selection (default TRUE).}

\item{direction}{Direction for stepwise: "both" (default), "backward", or "forward".}

\item{criterion}{"AIC" (default) or "BIC" (use for penalizing model size more).}

\item{add_diagnostics}{Logical; if TRUE, shows fitted vs actual and residual plots.}
}
\value{
Invisible list with model, summary, selected predictors, and plots.
}
\description{
Fits a multiple linear regression (lm) on the specified outcome and predictors,
with optional stepwise model selection. Visualizes fitted vs observed and residuals.
}
\examples{
df <- tibble::tibble(
  y = rnorm(100),
  x1 = rnorm(100),
  x2 = runif(100),
  x3 = sample(letters[1:3], 100, replace = TRUE)
)
auto_stepwise_regression_plot(df, outcome = "y", predictors = c("x1", "x2", "x3"))

}
