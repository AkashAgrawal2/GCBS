% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_stepwise_linear_regression.R
\name{auto_stepwise_linear_regression}
\alias{auto_stepwise_linear_regression}
\title{Robust Stepwise Multi-Predictor Linear Regression with Diagnostics and Report}
\usage{
auto_stepwise_linear_regression(
  data,
  outcome,
  predictors,
  interactions = FALSE,
  stepwise = TRUE,
  direction = c("both", "backward", "forward"),
  criterion = c("AIC", "BIC"),
  crossval = FALSE,
  k = 5,
  add_diagnostics = TRUE,
  report = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{outcome}{Name of numeric outcome variable.}

\item{predictors}{Character vector of predictor variable names.}

\item{interactions}{Logical: include all two-way interactions? Default FALSE.}

\item{stepwise}{Logical: Perform stepwise selection if TRUE (default).}

\item{direction}{"both" (default), "backward", or "forward".}

\item{criterion}{"AIC" (default) or "BIC".}

\item{crossval}{Logical; run k-fold cross-validation (default FALSE).}

\item{k}{Integer number of folds for CV (default 5, min 2).}

\item{add_diagnostics}{Logical; make diagnostic plots (default TRUE).}

\item{report}{Logical; print a markdown/HTML report (requires broom/knitr).}
}
\value{
Named list: model, summary, selected predictors, plots, VIF, cross-validation results, and optionally a markdown report.
}
\description{
Fits a linear regression model, with stepwise AIC/BIC selection, support for interactions, diagnostics (plots, VIF), and cross-validation.
}
\examples{
set.seed(3)
df <- tibble::tibble(
  y = 3 + 2*x1 + 0.2*x2 + ifelse(x3 == "b", 1, 0) + rnorm(100),
  x1 = rnorm(100), x2 = runif(100), x3 = sample(letters[1:3], 100, replace = TRUE)
)
auto_stepwise_linear_regression(df, "y", c("x1", "x2", "x3"), interactions = TRUE, crossval = TRUE)

}
