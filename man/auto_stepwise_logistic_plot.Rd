% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_stepwise_logistic_plot.R
\name{auto_stepwise_logistic_plot}
\alias{auto_stepwise_logistic_plot}
\title{Automatic Stepwise Multiple Logistic Regression with ROC Curve and Predicted Plot}
\usage{
auto_stepwise_logistic_plot(
  data,
  outcome,
  predictors,
  stepwise = TRUE,
  direction = c("both", "backward", "forward"),
  criterion = c("AIC", "BIC"),
  add_roc = TRUE
)
}
\arguments{
\item{data}{Data frame or tibble.}

\item{outcome}{Name of binary outcome variable (0/1 or 2-level factor; character).}

\item{predictors}{Character vector of predictors.}

\item{stepwise}{Logical; if TRUE, stepwise model selection by AIC/BIC.}

\item{direction}{Stepwise direction ("both", "backward", "forward"); default: "both".}

\item{criterion}{"AIC" (default) or "BIC".}

\item{add_roc}{Logical; add ROC curve plot.}
}
\value{
Invisible list with model, summary, predictors, plot(s).
}
\description{
Fits a multiple logistic regression (glm) on the binary outcome and given predictors,
with optional stepwise variable selection. Plots predicted probability and ROC curve.
}
\examples{
set.seed(2)
df <- tibble::tibble(
  y = factor(rbinom(100, 1, 0.3)),
  x1 = rnorm(100),
  x2 = sample(letters[1:3], 100, replace = TRUE)
)
auto_stepwise_logistic_plot(df, outcome = "y", predictors = c("x1", "x2"))

}
