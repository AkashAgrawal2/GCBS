% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_missing.R
\name{impute_missing}
\alias{impute_missing}
\title{Impute Missing Values in a Data Frame}
\usage{
impute_missing(data, impute_map = NULL)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{impute_map}{A named list where each name is a column to impute and each value is either:
\itemize{
\item a string (\code{"mean"}, \code{"median"}, \code{"mode"}, or \code{"constant"}) for method,
\item or a list of the form \code{list(method = "constant", constant = 5)}, etc.
If NULL (default), will impute all columns with missing data using \code{"mean"} for numerics or \code{"mode"} otherwise.
}}
}
\value{
Imputed tibble. Returns details of imputation as the \code{"impute_summary"} attribute.
}
\description{
Impute missing values in specified columns of a data frame using the mean, median, mode, or a constant value.
Allows specifying different imputation methods (and constants) for different columns.
Returns the imputed data frame (as a tibble), with details of changes as an attribute.
}
\examples{
df <- data.frame(
  age = c(20, NA, 22, 24, NA, 21),
  gender = c("M", "F", NA, "F", "F", "M"),
  income = c(50000, 45000, NA, 48000, 47000, NA)
)
impute_missing(df)
impute_missing(df, impute_map = list(
  age = list(method = "constant", constant = 30),
  gender = "mode",
  income = "median"
))
View summary of what was imputed:
attr(result, "impute_summary")

}
