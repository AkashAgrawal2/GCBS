% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_outliers.R
\name{detect_outliers}
\alias{detect_outliers}
\title{Detect and Optionally Plot Outliers in Numeric Columns}
\usage{
detect_outliers(
  data,
  columns = NULL,
  method = c("zscore", "iqr"),
  threshold = NULL,
  plot = TRUE,
  plot_type = c("boxplot", "scatter"),
  point_labels = NULL,
  quiet = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{columns}{Character vector of column names to check for outliers. Default: all numeric columns.}

\item{method}{Outlier detection method: \code{"zscore"} (default), \code{"iqr"}, or a custom function.}

\item{threshold}{Numeric. For z-score, absolute value above which a point is flagged (default: 3). For IQR, multiplier for IQR (default: 1.5).}

\item{plot}{Logical. Whether to plot the results for each \code{columns}. Default: FALSE.}

\item{plot_type}{"boxplot" or "scatter" (default "boxplot").}

\item{point_labels}{Optional. Column name (character) to use as plot point labels.}

\item{quiet}{Logical. If TRUE, suppresses all messages/warnings.}
}
\value{
The input tibble with additional \code{*_outlier} logical columns. Outlier summary is returned as the "outlier_summary" attribute.
}
\description{
Identifies outliers in specified numeric columns of a data frame using the z-score (default), IQR,
or a custom function. Optionally produces plots visualizing outliers in each column.
}
\examples{
df <- tibble::tibble(
  id = letters[1:6],
  height = c(160, 165, 170, 171, 168, 250),
  weight = c(60, 65, 62, 1000, 64, 61)
)
flagged <- detect_outliers(df, plot = TRUE, point_labels = "id")

}
