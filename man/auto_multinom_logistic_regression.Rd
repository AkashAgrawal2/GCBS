% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_multinom_logistic_regression.R
\name{auto_multinom_logistic_regression}
\alias{auto_multinom_logistic_regression}
\title{Stepwise Multinomial Logistic Regression with Diagnostics and Report}
\usage{
auto_multinom_logistic_regression(
  data,
  outcome,
  predictors,
  interactions = FALSE,
  stepwise = TRUE,
  direction = c("both", "backward", "forward"),
  criterion = c("AIC", "BIC"),
  crossval = FALSE,
  k = 5,
  report = FALSE
)
}
\arguments{
\item{data}{Data frame or tibble.}

\item{outcome}{Name of categorical outcome variable (must have 3+ levels).}

\item{predictors}{Character vector of predictors.}

\item{interactions}{Logical; include all two-way interactions? Default FALSE.}

\item{stepwise}{Logical; use stepwise AIC/BIC selection? Default TRUE.}

\item{direction}{"both" (default), "backward", or "forward".}

\item{criterion}{"AIC" (default) or "BIC".}

\item{crossval}{Logical; perform k-fold cross-validation (default FALSE).}

\item{k}{Number of folds for CV (default 5).}

\item{report}{Logical; print an HTML/Markdown report if TRUE.}
}
\value{
Named list with model, summary, coefficients (odds ratios and CIs), VIF, cross-validation results, plots, and markdown.
}
\description{
Fits a multinomial (nominal, >2-level) logistic regression with optional stepwise selection, VIF, cross-validation,
diagnostic plot of predicted class probabilities, and markdown report ready for teaching or publication.
}
\examples{
set.seed(101)
df <- tibble::tibble(
  y = factor(sample(c("A", "B", "C"), 120, replace=TRUE)),
  x1 = rnorm(120),
  x2 = runif(120),
  x3 = sample(letters[1:3], 120, replace=TRUE)
)
auto_multinom_logistic_regression(df, outcome = "y", predictors = c("x1", "x2", "x3"),
                     stepwise = FALSE, crossval = TRUE, k = 4)

}
