% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_stepwise_logistic_regression.R
\name{auto_stepwise_logistic_regression}
\alias{auto_stepwise_logistic_regression}
\title{Robust Stepwise Multi-Predictor Logistic Regression with ROC, Diagnostics, and Report}
\usage{
auto_stepwise_logistic_regression(
  data,
  outcome,
  predictors,
  interactions = FALSE,
  stepwise = TRUE,
  direction = c("both", "backward", "forward"),
  criterion = c("AIC", "BIC"),
  crossval = FALSE,
  k = 5,
  add_roc = TRUE,
  report = FALSE
)
}
\arguments{
\item{data}{Data frame or tibble.}

\item{outcome}{Name of binary outcome variable (0/1 or two-level factor).}

\item{predictors}{Character vector of predictors.}

\item{interactions}{Logical: include all two-way interactions? Default FALSE.}

\item{stepwise}{Use stepwise selection? Default TRUE.}

\item{direction}{"both" (default), "backward", or "forward".}

\item{criterion}{"AIC" or "BIC" (default AIC).}

\item{crossval}{Logical; run k-fold cross-validation for AUC (default FALSE).}

\item{k}{Folds for CV (default 5).}

\item{add_roc}{Logical: show ROC curve (default TRUE).}

\item{report}{Logical: print HTML summary (broom & knitr needed).}
}
\value{
Named list with model, summary, selected predictors, plots, VIF, cross-validation, report markdown.
}
\description{
Fits logistic regression (glm) with stepwise selection, supports interactions, VIF, cross-validation, and report.
}
\examples{
set.seed(3)
df <- tibble::tibble(
  y = factor(rbinom(150, 1, 0.4)),
  x1 = rnorm(150),
  x2 = runif(150),
  x3 = sample(letters[1:3], 150, replace = TRUE)
)
auto_stepwise_logistic_regression(df, "y", c("x1", "x2", "x3"), interactions = TRUE, crossval = TRUE)

}
